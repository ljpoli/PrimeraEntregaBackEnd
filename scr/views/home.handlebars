<div>
   <div class="text-center">
  <h1 class="fw-bold text-info">Lista de Productos</h1>
   </div>

           <div class="container">
  <div class="row">
    {{#each products}}
      <div class="col-md-3">
        <div class="card m-4" style="width: 18rem; height: 450px;">
                        <img src="{{this.thumbnails}}" class="card-img-top img-fluid" alt="{{this.name}}" style="max-height:300px;">
                        <div class="card-body">
                            <h5 class="card-title">{{this.title}}</h5>
                            <p class="card-text">Descripción: {{this.description}}.</p>
                            <h5>Price: ${{this.price}}</h5>
                            <p class="card-text">Categoria: {{this.category}}</p>
                        </div>
                    </div>
                </div>
            {{/each}}
    </div>
</div>